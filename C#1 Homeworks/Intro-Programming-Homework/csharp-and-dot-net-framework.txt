C# is a programming language,.NET is an application framework.Framework means it contained different applications.
The .NET libraries can run on the CLR and thus any language which can run on the CLR can also use the .NET libraries.
C# is designed for Common Language Infrastructure , which consists of the executable code and runtime environment