namespace BitArray
{
    using System;

    public class BitArrayMain
    {
        static void Main()
        {
            BitArray64 b1 =new  BitArray64(5u);
            BitArray64 b2 = new BitArray64(6u);
            
          //  Console.WriteLine(b1[0]);
         //   Console.WriteLine(b1==b2);
         //   Console.WriteLine(b1!=b2);
            Console.WriteLine(b.GetHashCode());

        }
    }

    // Define a class BitArray64 to hold 64 bit values inside an ulong value.Implement IEnumerable<int> and Equals(…), GetHashCode(), [], == and !=.

}
